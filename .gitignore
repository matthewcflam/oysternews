node_modules/
.next/
out/
.vercel/
.env
.env.local
*.tsbuildinfo
next-env.d.ts

# Phase 2's fake archive is a build artifact — regenerate with `npm run tiles:fake`.
# Phase 3 publishes the real one to Vercel Blob and never commits it either.
public/stories.pmtiles

# Copied out of node_modules by predev/prebuild so MapLibre's worker is a real
# static asset (scripts/copy-maplibre-worker.mjs). Versioned with the package,
# so committing them would just go stale.
public/maplibre-gl-worker.mjs
public/maplibre-gl-shared.mjs
.gstack/
